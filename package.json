require("dotenv").config();

const {
  Client,
  GatewayIntentBits,
  EmbedBuilder
} = require("discord.js");

const client = new Client({
  intents: [
    GatewayIntentBits.Guilds,
    GatewayIntentBits.GuildMessages,
    GatewayIntentBits.MessageContent
  ]
});

// ID do canal
const CHANNEL_ID = "1532295157897232464";

// ID do cargo
const ROLE_ID = "1532297126120263752";

client.once("ready", async () => {
  console.log(`${client.user.tag} está online!`);

  try {
    const channel = await client.channels.fetch(CHANNEL_ID);

    const embed = new EmbedBuilder()
      .setColor("#0099ff")
      .setTitle("🚀 Nova atualização disponível!")
      .setDescription("Confira a versão mais recente abaixo.")
      .addFields(
        {
          name: "📥 Download",
          value: "https://delta.filenetwork.vip/android.html"
        },
        {
          name: "📝 Changelog",
          value:
            "• Correções de bugs\n• Melhor desempenho\n• Novos recursos"
        }
      )
      .setTimestamp()
      .setFooter({
        text: "Roblox Scripts Bot"
      });

    await channel.send({
      content: `<@&${ROLE_ID}>`,
      embeds: [embed]
    });

  } catch (err) {
    console.log(err);
  }
});

client.on("messageCreate", async (message) => {
  if (message.author.bot) return;

  if (message.content === "!ping") {
    return message.reply("🏓 Pong!");
  }

  if (message.content === "!delta") {
    return message.reply("https://delta.filenetwork.vip/android.html");
  }

  if (message.content === "!teste") {

    const embed = new EmbedBuilder()
      .setColor("#0099ff")
      .setTitle("🚀 Nova atualização disponível!")
      .setDescription("Confira a versão mais recente abaixo.")
      .addFields(
        {
          name: "📥 Download",
          value: "https://delta.filenetwork.vip/android.html"
        },
        {
          name: "📝 Changelog",
          value:
            "• Correções de bugs\n• Melhor desempenho\n• Novos recursos"
        }
      )
      .setTimestamp()
      .setFooter({
        text: "Roblox Scripts Bot"
      });

    await message.channel.send({
      content: `<@&${ROLE_ID}>`,
      embeds: [embed]
    });
  }
});

client.login(process.env.TOKEN);
